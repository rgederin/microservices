spring:
  application:
    name: price-comparator-front

---
spring:
  profiles: default

  cloud:
    config:
      uri: http://localhost:8010

---
spring:
  profiles: prod

spring:
  cloud:
    config:
      uri: http://service-config:8010